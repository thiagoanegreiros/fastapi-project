{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Ruff - Format & Fix",
            "type": "shell",
            "command": "ruff format . && ruff check . --fix",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
